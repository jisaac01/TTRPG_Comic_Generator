Art direction:
{art_direction}

You are given a comic script. Rewrite the setting and visual_action fields of each panel so the descriptions clearly and consistently express the art direction above.

Hard constraints:
- Preserve story events, dialogue, held items, panel scale, panel shape, and panel order exactly.
- Preserve narrative_overlays_and_text_direction exactly (this list may be empty, which is fine).
- Rewrite only setting and visual_action.
- Every panel must be visibly rewritten.
- Every panel must include explicit style expression in both fields:
	- setting must include concrete rendering context (for example: lighting treatment, texture treatment, composition/framing, environmental density, or palette behavior).
	- visual_action must include concrete rendering behavior (for example: line quality, motion depiction, material treatment, atmospheric interaction, or contrast handling).
- Keep style intensity consistent across all panels, not concentrated in only one panel.
- Do not introduce style language that is not inferable from the provided art direction.
- Do not add new events or characters.

Return one rewritten panel object for every source panel, in the same order, using the same panel index. Every panel must be visibly rewritten; do not leave any panel's setting and visual_action unchanged.

Return only JSON with this shape:
{{
	"panels": [
		{{
			"index": 1,
			"setting": "rewritten setting",
			"visual_action": "rewritten visual action"
		}}
	]
}}

Panels:
{panels_context}