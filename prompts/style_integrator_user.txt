Art direction:
{art_direction}

You are given a comic script. Rewrite the setting and visual_action fields of each panel so the descriptions reflect the art style above. The story events, dialogue, held items, panel scale, panel shape, and panel order must remain exactly the same; only the visual language should change to match the art style.

For example: if the art style is "sketchy, hand-drawn with wobbly lines", and a panel says "A knight charges forward", rewrite it as "A scratchy, wobbly-lined knight lurches forward in a burst of uneven motion".

Return one rewritten panel object for every source panel, in the same order, using the same panel index. Every panel must be visibly rewritten; do not leave any panel's setting and visual_action unchanged.

Return only JSON with this shape:
{{
	"panels": [
		{{
			"index": 1,
			"setting": "rewritten setting",
			"visual_action": "rewritten visual action"
		}}
	]
}}

Keep rewrites concise. Do not add new events or characters.

Panels:
{panels_context}